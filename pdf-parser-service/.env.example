# Server Configuration
PORT=3000

# Authentication — Supabase JWT verification (HS256).
# Copy from Supabase Dashboard → Settings → API → JWT Secret.
# Must match the project the client app uses, otherwise verification fails.
SUPABASE_JWT_SECRET=

# Optional: Logging
NODE_ENV=production
LOG_LEVEL=info
